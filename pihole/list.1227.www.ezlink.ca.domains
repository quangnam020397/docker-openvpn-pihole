page=document.searchform2.page.value;
which=document.searchform2.cp.selectedindex;
which=document.searchform2.cp[which].value;
searchstring=document.searchform2.searchstring2.value;
searchstring.substring(i,i+1);
again.");
page=document.searchform.page.value;
which=document.searchform.cp.selectedindex;
which=document.searchform.cp[which].value;
searchstring=document.searchform.searchstring.value;
searchstring.substring(i,i+1);
again.");
